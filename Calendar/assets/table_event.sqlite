CREATE TABLE EVENT(
   ID INT PRIMARY KEY     autoincrement,
   Description           TEXT    NOT NULL,
   Category       INT     NOT NULL,
   StartDate       NUMERIC,
   EndDate         NUMERIC
);